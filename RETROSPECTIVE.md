# 프로젝트 회고 로그

## 2026-05-27 디자인 전체 리모델링

- 사용자 요청 요약: 기존 디자인이 촌스럽게 느껴져 문구와 기능은 유지하되, 설치된 디자인 스킬 기준으로 홈페이지와 공개 화면 전체 분위기를 과감하게 개선하길 원함. 작업 전 현재 상태의 완전 백업을 요청함.
- 진행한 문제 또는 요청: 백업 완료 후 공개 홈페이지, 헤더/푸터, 학생 목록, 학교 소개, 프로젝트 안내, 갤러리, 후원 신청 화면의 시각 체계를 리모델링.
- 확인한 원인과 단서: 기존 화면은 베이지/주황 계열이 과하게 반복되고 카드형 박스가 많아 정보 계층이 단조롭게 보였음. 모바일에서는 큰 제목 줄바꿈이 어색해질 수 있는 구조였음.
- 시도한 해결 과정: `design-taste-frontend`와 `web-design-guidelines` 기준을 확인하고, 비영리/교육 후원 레퍼런스에서 스토리 중심 이미지, 명확한 CTA, 차분한 단일 강조색, 넓은 여백 원칙을 적용함.
- 적용한 변경 사항: 전역 색상 토큰을 차콜/세이지/살구 포인트 조합으로 변경. 홈 히어로를 좌측 스토리와 우측 이미지/결연 현황 구조로 재구성. 헤더/푸터, 학생 카드, 필터, 공개 페이지 히어로, 신청 폼, 상태 뱃지의 시각 스타일을 통일.
- 검증 결과: `npm.cmd run -s lint` 통과. `npm.cmd run -s build` 통과. 로컬 `http://localhost:3000`에서 Edge 채널 Playwright 스크린샷으로 홈 데스크톱/모바일, 학생, 학교 소개, 프로젝트, 갤러리 화면 확인.
- 남은 위험 요소: 갤러리의 실제 업로드 이미지 품질이 낮거나 임시 이미지일 경우 디자인과 별개로 완성도가 떨어져 보일 수 있음. 모바일 하단 영역과 학생 상세 신청 플로우는 추가 실사용 QA가 필요함.
- 다음 단계: 배포 전 실제 운영 데이터가 있는 상태에서 주요 공개 페이지와 후원 신청 플로우를 한 번 더 확인. 사용자가 원하면 갤러리 이미지 썸네일 정책과 학생 카드 실존감 개선을 별도 작업으로 진행.

### 불편/개선 포인트

- 작업 단위를 너무 잘게 쪼개기보다, 큰 기능 단위로 구현과 테스트를 묶어 진행하는 편이 좋음.
- 디자인 작업은 즉흥적인 색/카드 조합보다 검증된 레퍼런스와 명확한 시각 원칙을 먼저 잡고 들어가는 것이 필요함.
- 작업 중 불편함과 개선 아이디어는 잊기 전에 즉시 회고 로그에 남기는 방식이 필요함.

## 2026-05-27 공개 UI 보정 및 장학금 지급관리 추가

- 사용자 요청 요약: 푸터 소개문 삭제, 홈 CTA 가시성 개선, 홈 메인 문구 크기 축소, 공개 연락처 수정 및 관리자 설정 연동, 학생 실명 관리/마스킹 공개명 적용, 손편지 명칭을 꿈편지로 변경, 장학금 지급관리 기능 추가를 요청함.
- 진행한 문제 또는 요청: 공개 페이지 문구/가시성 수정과 함께 관리자 학생 관리, 설정, 장학금 지급관리 메뉴를 확장함.
- 확인한 원인과 단서: 기존 학생 데이터는 닉네임만 저장하고 있었고, 장학금 지급 관련 개인정보/파일을 담을 별도 구조가 없었음. 연락처도 푸터에 고정 문구로 박혀 있어 설정 변경과 연동되지 않았음.
- 시도한 해결 과정: `students.real_name` 컬럼을 추가하고 기존 DB에는 런타임에서 컬럼을 보강하도록 처리. 장학금 지급정보는 `student_scholarship_records` 별도 테이블로 분리. 공개 푸터는 설정값을 읽어 대표 연락/후원 상담 번호를 표시하도록 변경.
- 적용한 변경 사항: 공개 학생명은 실명이 있으면 `이*진` 형식으로 마스킹. 관리자 학생 관리에서 실명 입력/수정 가능. 모든 사용자 노출 `손편지` 문구를 `꿈편지`로 변경. 장학금 지급관리 메뉴와 화면 추가, 학생별 장학금 구분/연락처/학부모/계좌/등본/통장 파일 관리 추가. 기본 배정은 학생 수 기준 전액 20%, 반액 30%, 부분 50%로 계산.
- 검증 결과: `npm.cmd run -s lint` 통과. `npm.cmd run -s build` 통과. 빌드 결과 `/admin/scholarships` 라우트 생성 확인.
- 남은 위험 요소: 주민등록등본/통장 파일은 민감자료이므로 운영에서는 관리자 접근권한, Blob URL 공개 범위, 파일 삭제 정책을 추가로 점검해야 함. 기존 운영 DB에는 `npm run db:schema` 또는 앱 런타임 접근을 통해 새 테이블/컬럼 반영이 필요함.
- 다음 단계: 실제 운영 DB에서 장학금 지급관리 저장/파일 업로드를 한 번 테스트하고, 개인정보 파일 접근 정책을 운영 기준에 맞게 확정.

## 2026-05-27 문자 템플릿 수동 발송 입력 단순화

- 사용자 요청 요약: `후원 신청 접수 안내(sponsorship_received)` 수동 발송 화면에 `studentNickname`, `amount`, `period` 등 불필요한 변수 입력칸이 너무 많이 보여 혼란스럽다고 지적함. 수신번호와 이름만 남기길 요청함.
- 진행한 문제 또는 요청: 후원 신청 접수 안내 템플릿 본문과 수동 발송 UI 입력 필드 정리.
- 확인한 원인과 단서: 기존 수동 발송 UI가 모든 템플릿에 동일한 변수 입력칸을 무조건 렌더링하고 있었음. `sponsorship_received` 본문도 학생명/금액/기간 변수를 요구하고 있었음.
- 시도한 해결 과정: 템플릿 정의에 수동 발송용 필드 목록을 추가하고, UI가 템플릿별 필요한 필드만 렌더링하도록 변경.
- 적용한 변경 사항: `sponsorship_received` 본문을 `{{name}}님, 후원 신청이 접수되었습니다. 관리자 확인 후 연락드리겠습니다.`로 단순화. 해당 템플릿 수동 발송 입력은 `수신번호`, `이름`만 표시. 버튼 문구는 `수동 발송`에서 `문자 발송`으로 변경.
- 검증 결과: `npm.cmd run -s lint` 통과. `npm.cmd run -s build` 통과.
- 남은 위험 요소: 자동 발송에서는 학생별 후원 맥락이 서버 데이터로 연결되지만, 수동 발송은 운영자가 입력한 값 기준으로만 발송되므로 실제 운영 전 템플릿별 문구를 한 번 더 확인할 필요가 있음.
- 다음 단계: 관리자 `/admin/messages`에서 실제 화면으로 템플릿 입력칸이 의도대로 줄었는지 확인.

## 2026-05-27 관리자 학생/설정 화면 운영 위험 요소 제거

- 사용자 요청 요약: `전체 학생 신청 가능 상태로 초기화` 버튼은 운영상 위험하므로 UI에서 제거하고, 학생 실명 예시 `이경진`은 부적절하니 `홍길동`으로 변경 요청. `관리자 안내 전화번호` 기능의 용도 확인 요청.
- 진행한 문제 또는 요청: 학생 관리 화면의 위험 버튼 제거, 실명 placeholder 정리, 설정 화면의 미사용 연락처 항목 제거.
- 확인한 원인과 단서: `관리자 안내 전화번호(admin_contact_phone)`는 기존 설정 구조에만 남아 있고 공개 페이지/문자 발송 로직에서 실제 사용되지 않았음. 전체 상태 초기화 서버 액션은 존재하지만 UI 노출은 운영 사고 가능성이 큼.
- 시도한 해결 과정: 학생 관리 컴포넌트에서 초기화 버튼과 관련 상태/핸들러를 제거. 실명 예시는 `홍길동`으로 변경. 설정 화면과 저장 액션에서 미사용 `관리자 안내 전화번호` 입력/저장을 제거.
- 적용한 변경 사항: 관리자 UI에서 전체 상태 초기화 버튼 제거, `이경진` 예시 제거, 설정 화면에는 실제 공개 푸터에 쓰이는 `대표 연락`, `후원 상담`과 문자 관련 값만 남김.
- 검증 결과: `npm.cmd run -s lint` 통과. `npm.cmd run -s build` 통과.
- 남은 위험 요소: 서버 액션 `resetAllStudentsToAvailableAction`은 비노출 상태로 남아 있어 필요 시 개발자/관리 요청으로만 실행 가능. 완전히 삭제하려면 별도 정리 가능.
- 다음 단계: 관리자 설정 화면에서 불필요한 연락처 항목이 사라졌는지 실제 화면 확인.

## 2026-05-27 결연 신청서 필수 입력 오류 표시 개선

- 사용자 요청 요약: 결연 신청서 작성 페이지에서 휴대폰 번호, 개인정보 수집 및 이용 동의 같은 필수 오류 메시지가 눈에 잘 들어오지 않아 중앙에 큰 경고창으로 표시하고, 확인 버튼을 누르면 사라지게 변경 요청.
- 진행한 문제 또는 요청: 기존 작은 안내 메시지만으로는 사용자가 제출 실패 원인을 즉시 인지하기 어려운 문제를 개선.
- 확인한 원인과 단서: `components/public/sponsorship-request-form.tsx`에서 서버 액션 결과의 `fieldErrors`, `formError`를 필드 근처와 토스트 형태로만 표시하고 있었음.
- 시도한 해결 과정: 서버 검증 로직과 문구는 유지하고, 클라이언트 표시 방식만 중앙 모달형 경고창으로 변경.
- 적용한 변경 사항: 오류가 있으면 화면 중앙에 `신청서 확인이 필요합니다` 경고창을 띄우고, 오류 내용을 크게 묶어 보여주며, 말풍선 아래의 `확인` 버튼으로 닫히도록 처리. 닫은 뒤에도 각 필드 아래 오류 문구는 남겨 사용자가 수정 위치를 확인할 수 있게 유지.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 남은 위험 요소: 실제 브라우저에서 모바일 화면 기준으로 경고창 높이와 버튼 위치가 사용자가 원하는 감각과 맞는지 최종 시각 확인 필요.
- 다음 단계: 결연 신청서에서 휴대폰 번호 누락, 개인정보 동의 미체크 상태로 제출해 중앙 경고창 표시와 확인 버튼 닫힘 동작 확인.

## 2026-05-27 관리자 대시보드 모바일 전화 걸기 개선

- 사용자 요청 요약: 관리자 대시보드 `오늘 전화할 후원자` 목록에서 후원자 전화번호 옆 `전화번호 복사` 버튼을 휴대폰에서는 `전화 걸기`로 바꾸고, 누르면 휴대폰 전화 앱에 번호가 입력된 상태가 되게 요청.
- 진행한 문제 또는 요청: 데스크톱 운영에서는 복사가 편하지만, 모바일 운영에서는 바로 전화 연결 흐름이 더 적합한 문제를 개선.
- 확인한 원인과 단서: `app/admin/(panel)/dashboard/page.tsx`의 해당 목록에서 공용 `CopyButton`을 그대로 사용하고 있었음.
- 시도한 해결 과정: 공용 복사 버튼은 다른 화면 영향이 있어 유지하고, 대시보드 해당 위치에서만 반응형으로 데스크톱 복사 버튼과 모바일 전화 링크를 분기.
- 적용한 변경 사항: `sm` 이상 화면에서는 기존 `전화번호 복사` 버튼을 표시하고, 모바일에서는 `전화 걸기` 링크를 표시. 링크는 `tel:` 형식으로 연결하며 전화번호의 하이픈/공백은 제거해 전화 앱에 전달.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 남은 위험 요소: 실제 모바일 브라우저에서 전화 앱 실행은 기기/브라우저 권한 정책에 따라 다를 수 있어 실기기 확인 필요.
- 다음 단계: 휴대폰에서 관리자 대시보드 접속 후 `오늘 전화할 후원자` 목록의 `전화 걸기` 버튼을 눌러 전화 앱에 번호가 입력되는지 확인.

## 2026-05-27 장학금 군 기준 후원 제도 반영

- 사용자 요청 요약: 후원 신청서 금액을 학생별 장학금 군에 맞춰 고정하고, 장학금 지급관리에서 전액/반액/부분 장학금 배정 현황과 비율 초과 경고를 관리하고 싶다고 요청.
- 진행한 문제 또는 요청: 기존 신청서는 모든 학생에게 10만 원 후원 선택지를 보여주고 있었고, 장학금 지급관리 요약은 배정 인원만 보여 기준 인원 대비 초과 여부를 바로 판단하기 어려웠음.
- 확인한 원인과 단서: 학생별 장학금 군 저장 구조는 `student_scholarship_records`에 이미 있었으나, 공개 신청서와 문자 금액 해석은 장학금 군과 연결되지 않았음.
- 시도한 해결 과정: 기존 신청/문자 DB 구조는 유지하고, 장학금 군 계산 함수를 공용화한 뒤 관리자 화면과 공개 신청서 표시 금액, 문자 금액 해석에 연결.
- 적용한 변경 사항: 장학금 지급관리 요약 카드에 기준 인원, 현재 배정 인원, 배정 퍼센트를 추가. 전액/반액 장학금으로 변경 시 기준 인원을 초과하면 중앙 확인창을 띄우고 `무시하고 설정` 또는 `취소`를 선택하게 변경. 공개 신청서는 학생 장학금 군 기준 월 100,000원/50,000원/30,000원을 고정 표시하고 정기후원으로 접수되게 변경. 문자 금액도 해당 학생의 장학금 군 금액을 기준으로 해석하도록 보정.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. 로컬 `/admin/scholarships`에서 기준 인원/% 배정 문구 응답 확인, `/students/st-004/sponsorship`에서 후원 금액/정기후원 문구 응답 확인.
- 남은 위험 요소: 초과 경고 모달은 실제 관리자 조작으로 저장 직전 흐름을 확인해야 함. 기존 접수 건에는 별도 금액 컬럼이 없으므로 과거 신청의 금액 표시는 현재 학생 장학금 군 기준으로 해석됨.
- 다음 단계: 관리자에서 전액/반액 기준을 일부러 초과하도록 변경해 경고창과 `무시하고 설정` 저장 흐름을 실사용 화면에서 확인.

## 2026-05-27 설정 화면 운영 정책 제거 및 학생 성별 인원 확인

- 사용자 요청 요약: `전체 목표 학생 수`, `기본 후원 금액`, 공개 사이트 공개 여부, 후원 가능 여부는 장학금 결연 구조에서는 의미가 없으므로 설정에서 제거 요청. 학생 실명 등록 전 현재 학생 61명의 남/여 인원 확인 요청.
- 진행한 문제 또는 요청: 설정값 중 실제 운영에 필요 없는 운영 정책 항목을 제거하고, 메인 진행률이 목표 학생 수 설정에 의존하지 않게 정리.
- 확인한 원인과 단서: `target_student_count`는 공개 메인 진행률의 분모 fallback으로만 사용되고 있었음. 실제 DB 기준 학생 수는 61명이며 성별은 남 29명, 여 28명, 미정 4명.
- 시도한 해결 과정: 설정 폼/저장 액션/설정 기본값 타입에서 운영 정책 필드를 제거하고, 공개 메인 진행률은 실제 등록 학생 수만 기준으로 계산하게 변경.
- 적용한 변경 사항: 관리자 설정 화면에서 운영 정책 설정 섹션 제거. `전체 목표 학생 수`, `기본 후원 금액`, `사이트 공개 여부`, `후원 요청 가능 여부` 입력/검증/저장 제거. 공개 푸터와 설정 문구의 `후원 상담`은 `장학금 결연 상담`으로 변경.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. 로컬 `/admin/settings` 응답에서 제거 대상 문구 미노출 확인.
- 남은 위험 요소: DB `settings` 테이블에는 과거 설정 키가 남아 있을 수 있으나 코드에서 더 이상 읽거나 저장하지 않음. 전체 공개 문구의 `후원` 용어는 화면별 의미를 보며 추가 치환 필요.
- 다음 단계: 사용자가 제공할 남/여 실명 목록을 기준으로 학생 실명 일괄 등록 후 닉네임 시스템 제거 범위 결정.

## 2026-05-27 학생 성별 `미정` 제거

- 사용자 요청 요약: 학생 성별 선택지에서 `미정`을 없애고, 남/여 둘 중 하나만 설정되게 요청.
- 진행한 문제 또는 요청: 관리자 학생 추가/수정 및 공개 필터에 `미정` 선택지가 남아 있어 성별 데이터가 불명확해지는 문제를 정리.
- 확인한 원인과 단서: 기존 DB에는 `미정` 학생 4명이 있었고, 해당 학생들의 프로필 이미지 경로는 모두 `/students/profiles/female/...` 쪽이었음.
- 시도한 해결 과정: 타입, 서버 검증, 관리자 UI, 공개 필터, seed/schema에서 `미정`을 제거하고 기존 DB의 4명은 이미지 경로 기준으로 `여`로 변경.
- 적용한 변경 사항: `StudentGender`를 `남 | 여`로 제한. 관리자 학생 추가/수정 select와 공개 학생 필터에서 `미정` 옵션 제거. 신규 seed 데이터의 `미정` 값을 `여`로 변경. 실제 DB의 `st-015`, `st-030`, `st-045`, `st-060` 성별을 `여`로 업데이트.
- 검증 결과: 실제 DB 성별 카운트가 총 61명 중 남 29명, 여 32명으로 정리됨. 코드 검색에서 `미정` 잔여 문구 없음. `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 남은 위험 요소: 기존 DB 테이블의 CHECK 제약은 물리적으로 `미정`을 허용하는 형태가 남아 있을 수 있으나, 앱 코드와 UI에서는 더 이상 입력할 수 없음.
- 다음 단계: 필요 시 DB 테이블 재작성 마이그레이션으로 CHECK 제약 자체도 `남/여`만 허용하도록 물리 정리.

## 2026-05-27 학생 실명 배정 및 공개명 마스킹

- 사용자 요청 요약: 제공한 남학생 29명, 여학생 32명 이름을 성별에 맞춰 학생 실명으로 넣고, 기존 닉네임 칸은 실명 가운데 글자를 가린 공개명으로 변경 요청. 공개 메인 `후원자 응원 메시지` 제목 변경과 후원자명 마스킹도 요청.
- 진행한 문제 또는 요청: 실제 학생 이름 데이터 등록과 공개 화면 개인정보 노출 방지 규칙 정리.
- 확인한 원인과 단서: 현재 DB 학생 수는 총 61명, 남 29명/여 32명이며 실명 미등록 상태였음. 기존 마스킹 함수는 네 글자 이상 이름에서도 별표 1개만 넣고 있었음.
- 시도한 해결 과정: DB 업데이트 전 `backups/student-name-assignment-before-2026-05-27T05-46-57-041Z.json`에 기존 학생 이름 데이터를 백업하고, 성별별 학생 정렬 순서에 맞춰 실명과 마스킹 닉네임을 일괄 저장.
- 적용한 변경 사항: 모든 학생 `real_name` 입력 완료, `nickname`은 `김*연`, `이**우`처럼 가운데 글자 수만큼 `*`로 저장. 마스킹 함수도 동일 규칙으로 수정. 공개 메인 제목을 `아이들의 내일을 함께 만들어가는 마음`으로 변경하고, 공개 후원자명도 같은 방식으로 마스킹.
- 검증 결과: 실명 미등록 학생 0명 확인. DB 성별 카운트 남 29명/여 32명 유지. 로컬 메인 HTML에서 새 제목과 마스킹 이름 노출 확인. `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 남은 위험 요소: seed 파일에는 전체 실명 목록이 아직 반영되지 않아 DB 재시드 시 실명 데이터는 별도 일괄 등록이 필요할 수 있음.
- 다음 단계: 닉네임 시스템 제거 범위를 확정하고 관리자/공개 화면의 용어를 `실명/공개명` 기준으로 정리.

## 2026-05-27 대표 학생 영역 문구 제거 및 학생 기본 정렬 변경

- 사용자 요청 요약: 홈의 `함께 만나볼 학생들` 영역에서 `대표 학생 카드` 문구를 제거하고, 학생 기본 정렬을 신청가능, 입금대기, 결연완료 순으로 변경 요청.
- 진행한 문제 또는 요청: 공개 홈 대표 학생 추출과 공개 학생 목록 기본 정렬이 기존 DB 순서에 가까워 신청 가능한 학생이 항상 먼저 보장되지 않았음.
- 확인한 원인과 단서: 공개 학생 목록의 `default` 정렬은 기존 인덱스 순서였고, 홈 대표 학생은 `students.slice(0, 3)`으로 가져오고 있었음.
- 시도한 해결 과정: 학생 상태 정렬 가중치를 공용 함수로 분리하고, 홈 대표 학생과 공개 학생 목록 기본 정렬에 동일하게 적용.
- 적용한 변경 사항: 기본 정렬 순서를 `신청가능 -> 입금대기 -> 결연완료`로 변경. 공개 학생 목록 정렬 옵션 문구를 `기본순 (신청 가능 우선)`으로 변경. 홈 `대표 학생 카드` 라벨과 빈 상태 설명의 해당 문구 제거.
- 검증 결과: 로컬 홈 응답에서 `대표 학생 카드` 문구 미노출 및 `함께 만나볼 학생들` 유지 확인. 공개 학생 목록에서 `기본순 (신청 가능 우선)` 문구 확인. `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 남은 위험 요소: 같은 상태 안에서는 기존 DB 정렬을 유지하므로, 세부 순서까지 바꾸려면 별도 기준이 필요함.
- 다음 단계: 필요 시 관리자 학생 관리 화면도 같은 상태 우선 정렬로 맞출지 결정.

## 2026-05-27 공개 페이지 상단 보조 라벨 제거

- 사용자 요청 요약: 메인/학생/학교소개/프로젝트 안내 페이지의 초록색 작은 보조 라벨을 제거 요청.
- 진행한 문제 또는 요청: 본문 제목 위에 반복적으로 붙는 작은 라벨이 화면을 산만하게 만드는 문제를 정리.
- 확인한 원인과 단서: 메인 히어로의 `해밀학교 생활관비 1:1 결연 후원`, 학생 페이지 히어로의 `학생 만나기`, 학생 필터 카드의 `학생 찾기 필터`, 학교 소개 히어로/본문의 `학교 소개`/`교육 철학`, 프로젝트 히어로의 `프로젝트 안내`가 별도 문구로 렌더링되고 있었음.
- 시도한 해결 과정: 네비게이션 메뉴명과 SEO 메타데이터는 유지하고, 사용자가 지적한 본문 시각 라벨만 제거.
- 적용한 변경 사항: 해당 보조 라벨 `<p>` 요소 제거 및 제목 상단 여백 보정.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 재실행 결과 종료 코드 0으로 통과. 로컬 응답에서 메인 작은 라벨과 학생 필터 라벨 미노출 확인.
- 남은 위험 요소: `학생 만나기`, `학교 소개`, `프로젝트 안내` 문구는 네비게이션/메타데이터에는 남아 있음. 이는 페이지 이동 메뉴와 SEO 목적이라 본문 라벨 제거 범위와 별개.
- 다음 단계: 네비게이션 문구까지 장학금 결연 톤으로 바꿀지 별도 결정.

## 2026-05-27 프로젝트 안내 후원 절차 화살표 보정

- 사용자 요청 요약: 후원 절차 아래 방향 화살표의 가운데 픽셀이 비어 보이므로 초록색으로 꽉 차 보이게 하고, 막대기 길이를 아래로 더 늘려달라고 요청.
- 진행한 문제 또는 요청: 기존 화살표가 stroke 선 2개로 구성되어 중앙 접합부가 비어 보이는 시각 문제를 수정.
- 확인한 원인과 단서: `app/(public)/project/page.tsx`의 후원 절차 연결 화살표 SVG가 `stroke` 기반으로 그려져 있었음.
- 시도한 해결 과정: 선 기반 SVG를 채워진 `rect`와 `path` 조합으로 변경.
- 적용한 변경 사항: 화살표 줄기를 더 길게 내리고, 머리 부분을 `fill="#486f5b"` 도형으로 처리해 가운데가 비어 보이지 않게 변경.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. 로컬 `/project` 응답에서 새 SVG viewBox 확인.
- 남은 위험 요소: 실제 화면에서 사용자가 원하는 막대 길이 감각은 브라우저 시각 확인 후 추가 미세 조정 가능.
- 다음 단계: 사용자가 화면 확인 후 더 길게/더 두껍게 원하면 SVG 수치만 추가 조정.

## 2026-05-27 갤러리 페이지 보조 라벨 제거

- 사용자 요청 요약: 갤러리 페이지 `학교 활동 기록` 위에 있는 작은 `갤러리` 라벨 제거 요청.
- 진행한 문제 또는 요청: 다른 공개 페이지와 동일하게 히어로 상단 보조 라벨을 정리.
- 확인한 원인과 단서: `app/(public)/gallery/page.tsx` 히어로 영역에 `갤러리` 텍스트가 별도 `<p>`로 렌더링되고 있었음.
- 적용한 변경 사항: 히어로 보조 라벨 제거, `학교 활동 기록` 제목은 유지하고 상단 여백 보정.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. 로컬 `/gallery`에서 `학교 활동 기록` 유지 확인.
- 남은 위험 요소: `갤러리` 문구는 네비게이션/메타데이터에는 남아 있음. 이는 화면 상단 보조 라벨 제거 범위와 별개.
- 다음 단계: 네비게이션 문구까지 변경할지 별도 결정.

## 2026-05-27 후원자 응원 말풍선 테스트 UI 추가

- 사용자 요청 요약: 메인 페이지 `아이들의 내일을 함께 만들어가는 마음` 영역을 더 크게 만들고, 후원자 응원 더미 메시지 10개를 추가해 말풍선 4개씩 보이게 하며 약 6초마다 다음 4개로 스윽 전환되게 요청.
- 진행한 문제 또는 요청: 후원자 응원 영역의 시각 비중과 메시지 노출 방식 테스트.
- 확인한 원인과 단서: 기존 메인 영역은 메시지를 단순 목록으로 보여주고 있었고, 실제 공개 메시지가 적으면 UI 밀도와 전환 효과를 확인하기 어려웠음.
- 시도한 해결 과정: 실제 DB에는 더미를 넣지 않고, 코드 상단의 `TEST_DUMMY_SPONSOR_MESSAGES` 상수로 테스트 더미 10개를 구성. 나중에 삭제하기 쉽도록 주석으로 테스트용임을 표시.
- 적용한 변경 사항: `components/public/sponsor-message-carousel.tsx` 추가. 4개 말풍선을 보여주고 6초마다 다음 4개로 전환. `prefers-reduced-motion` 환경에서는 자동 전환을 멈춤. 메인 응원 영역 크기와 제목 크기를 확대하고 설명 문구 추가.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. 로컬 메인 HTML에서 더미 메시지와 새 설명 문구 확인. Playwright Edge 스크린샷 생성: `C:\Users\juqkfkrl\AppData\Local\Temp\haemil-home-sponsor-messages.png`.
- 남은 위험 요소: `TEST_DUMMY_SPONSOR_MESSAGES`는 테스트용 더미이므로 실제 운영 전 삭제해야 함. 테스트 종료 시 사용자 요청대로 이 배열과 병합 로직을 제거해야 함.
- 다음 단계: 사용자가 화면 확인 후 효과가 괜찮다고 하면 더미 제거 후 실제 공개 메시지만 캐러셀에 연결.

## 2026-05-27 후원 절차 화살표 방향 재조정

- 사용자 요청 요약: 이전 화살표 수정 후 공간이 더 많이 생겼고, 늘려야 하는 방향이 반대였다고 피드백.
- 진행한 문제 또는 요청: 화살표를 아래쪽으로 키우면서 머리 아래 여백이 커져 보이는 문제 보정.
- 적용한 변경 사항: SVG 전체 높이를 다시 줄이고, 줄기와 화살표 머리의 접점이 더 가까워지도록 viewBox와 rect/path 좌표를 재조정.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. 로컬 `/project` 응답에서 조정된 SVG 확인.
- 남은 위험 요소: 아이콘 픽셀 감각은 실제 브라우저 화면 확인 후 추가 미세 조정 가능.

## 2026-05-27 후원자 응원 섹션 배경형 레이아웃 변경

- 사용자 요청 요약: 후원자 응원 섹션을 사진이 둥근 구역 전체 배경으로 깔리는 구조로 바꾸고, 사진을 어둡고 배경처럼 처리한 뒤 문구는 위 중앙, 말풍선 4개는 아래 가로 정렬로 배치 요청. 문구 전환 속도도 더 느리게 조정 요청.
- 진행한 문제 또는 요청: 기존 구조는 좌측 문구/우측 말풍선/하단 이미지가 분리되어 사용자가 원하는 배경형 구성과 달랐음.
- 적용한 변경 사항: 메인 응원 섹션 전체에 학교 사진을 배경으로 깔고 `saturate`/`brightness`/어두운 오버레이를 적용. 제목과 설명은 중앙 상단, 말풍선 4개는 하단 중앙의 4열 그리드로 배치. 전환 간격은 6초에서 9초로 늘리고 애니메이션 시간도 0.9초로 조정.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. 로컬 메인 응답에서 배경 이미지 효과 클래스와 테스트 더미 메시지 확인. Playwright Edge 스크린샷 생성: `C:\Users\juqkfkrl\AppData\Local\Temp\haemil-home-sponsor-bg.png`.
- 남은 위험 요소: `TEST_DUMMY_SPONSOR_MESSAGES`는 계속 테스트용으로 남아 있으므로 운영 전 삭제 필요.

## 2026-05-27 후원자 응원 섹션 배경/전환 속도 추가 조정

- 사용자 요청 요약: 배경 사진을 조금 더 어둡게 하고, 말풍선 애니메이션을 과감하게 더 느리게 조정 요청.
- 적용한 변경 사항: 배경 이미지 `brightness`를 `0.42`에서 `0.32`로 낮추고 채도도 `0.62`로 낮춤. 어두운 오버레이를 `52%`로 강화. 말풍선 전환 간격을 `9초`에서 `14초`로 늘리고, 전환 애니메이션 시간을 `1.6초`로 조정.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 남은 위험 요소: 실제 체감 속도는 화면 확인 후 추가 조정 가능.

## 2026-05-27 공개 페이지 본문 가독성 강화

- 사용자 요청 요약: 학생 운영 안내, 학생 카드 소개, 학교 소개/프로젝트 본문 등 긴 설명 문구가 너무 연해서 잘 보이지 않는다고 지적. 색 진하기를 확 올려달라고 요청.
- 진행한 문제 또는 요청: 공개 페이지 주요 설명 텍스트의 대비와 시인성 개선.
- 확인한 원인과 단서: 주요 본문이 `#4b5a53`, `#63706a`, `#314039` 등 낮은 대비의 보조 색상으로 많이 쓰이고 있었음.
- 적용한 변경 사항: 학생 페이지 운영 안내, 학생 카드 설명/보조 문구, 학교 소개 본문, 프로젝트 안내 본문, 메인 히어로 설명, 학생 필터 안내 텍스트를 더 진한 `#1f2b25` 또는 `#314039` 계열로 올리고 `font-semibold`/`font-bold`를 적용.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. 로컬 `/students`, `/about`, `/project` 응답에서 진한 텍스트 클래스 반영 확인.
- 남은 위험 요소: 일부 빈 상태/보조 UI 문구는 의도적으로 덜 강조된 색을 유지함. 사용자가 더 진하게 원하면 해당 보조 문구도 추가 조정 가능.

## 2026-05-27 결연 신청서 상단 정리 및 3년 결연 고정

- 사용자 요청 요약: 결연 신청서 상단의 작은 초록색 `결연 신청`, 큰 설명 문구, 상태/입금대기/결연완료 안내를 지우고, 사진 옆에 `{학생명} 학생 결연 신청서`만 크게 남기길 요청. 또한 결연 구조를 구독형 기간 선택이 아니라 3년 단위 결연으로 반영 요청.
- 진행한 문제 또는 요청: 신청서 상단이 운영 설명 중심으로 복잡했고, 기간 선택 UI가 3년 결연 정책과 맞지 않았음.
- 적용한 변경 사항: 신청서 상단 헤더/설명 제거. 학생 사진 카드에 큰 `학생 결연 신청서` 제목만 남김. 후원 기간 선택/직접 입력 UI 제거. 공개 신청서 제출값은 `정기후원 + 3년`으로 고정하고, 서버 처리에서도 기간을 `3년`으로 강제. 금액 카드 문구를 `3년 결연 장학금`으로 변경. 프로젝트/학생 안내 문구도 3년 결연 중심으로 일부 갱신.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. 로컬 신청서에서 기존 상단 문구/상태 안내 미노출, `학생 결연 신청서`와 `3년 결연 장학금` 노출 확인. 프로젝트 페이지에서 자동 결제 문구 미노출 확인.
- 남은 위험 요소: DB 컬럼명과 기존 내부 값 `정기후원`은 호환성을 위해 유지 중. 관리자 테이블의 과거 신청 데이터에는 기존 기간 값이 남아 있을 수 있음.
- 다음 단계: 관리자 신청 관리 화면의 `후원 방식/후원 기간` 용어도 `결연 방식/결연 기간`으로 정리할지 결정.

## 2026-05-27 결연 신청서 금액 카드 보조 라벨 제거

- 사용자 요청 요약: 결연 신청서 금액 카드에 새로 들어간 초록색 `3년 결연 장학금` 보조 라벨 제거 요청.
- 적용한 변경 사항: 금액 카드 상단 보조 라벨 삭제. 월 금액과 `3년 결연` 배지만 유지.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. 로컬 신청서 응답에서 `3년 결연 장학금` 문구 미노출 확인.

## 2026-05-27 결연 신청서 상단 학생 카드 크기 축소

- 사용자 요청 요약: 결연 신청서 상단의 학생 사진/제목 말풍선 카드가 너무 커서, 전처럼 사진과 제목을 왼쪽에서 오른쪽으로 읽히게 하되 카드 크기는 내용에 꽉 맞게 더 줄여달라고 요청.
- 진행한 문제 또는 요청: 상단 카드의 불필요한 좌우 여백과 큰 카드 폭을 줄이고, 사진 옆 제목 구조는 유지.
- 확인한 원인과 단서: 기존 카드가 넓은 영역을 차지해 제목 대비 빈 공간이 커 보였고, 사용자는 중앙 정렬보다 컴팩트한 가로형 카드를 선호.
- 적용한 변경 사항: `app/(public)/students/[studentId]/sponsorship/page.tsx`의 상단 카드를 `w-fit max-w-full` 기반으로 변경하고, 그리드 폭을 `56px/64px` 기준으로 축소. 사진 크기는 `w-14 sm:w-16`, 카드 패딩은 `px-4 py-3`, 제목은 `text-xl sm:text-2xl`로 조정.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 남은 위험 요소: 실제 브라우저에서 학생 이름 길이가 긴 경우 제목 줄바꿈 느낌은 추가 확인 가능.
- 다음 단계: 사용자가 화면에서 아직 크다고 느끼면 사진 크기와 제목 크기를 한 단계 더 낮춰 추가 조정.

## 2026-05-27 결연 신청 완료 화면 문구 정리

- 사용자 요청 요약: 결연 완료 화면의 초록색 `신청 완료` 라벨을 제거하고, 완료 안내 문구를 더 자연스러운 해밀학교 운영 안내 톤으로 변경 요청.
- 진행한 문제 또는 요청: 신청 완료 화면의 불필요한 보조 라벨 제거 및 본문 안내 문구 교체.
- 적용한 변경 사항: `app/(public)/students/[studentId]/sponsorship/page.tsx`에서 `신청 완료` 라벨 삭제. 본문 문구를 `해밀학교에서 내용을 확인한 후...` 문장으로 변경하고, 안내 박스 문구를 `오랜 시간 따뜻한 연결` 및 `차근차근 설명` 톤으로 교체.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 다음 단계: 실제 완료 화면에서 문단 간격과 카드 높이가 과하면 추가로 패딩/행간 미세 조정.

## 2026-05-27 푸터 평일 상담 시간 표기 안정화

- 사용자 요청 요약: 푸터 `평일 상담: 09:00 - 18:00`의 시간과 하이픈 위치가 왼쪽으로 쏠리고 콜론이 잘 보이지 않아 더 안정적으로 보이게 수정 요청.
- 적용한 변경 사항: `components/public/public-footer.tsx`에서 평일 상담 시간을 일반 문장 대신 작은 시간 배지로 분리. `09:00`, `-`, `18:00`을 동일 폭 그리드 칸에 넣고 `font-mono`, `tabular-nums`를 적용해 숫자와 구분자 위치가 흔들리지 않도록 조정.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 다음 단계: 실제 화면에서 배지가 너무 도드라져 보이면 배경 투명도나 패딩을 한 단계 낮춰 조정.

### 후속 조정

- 사용자 추가 요청: 평일 상담 시간에 생긴 동그란 배지 형태가 별로라 제거하고, 기존처럼 텍스트만 있는 깔끔한 형태 요청.
- 적용한 변경 사항: 시간 배지의 배경, 테두리, 라운드, 패딩을 제거. `평일 상담:` 텍스트는 유지하고 `09:00 - 18:00`만 고정폭 숫자 그리드로 정렬해 콜론과 하이픈 위치가 안정적으로 보이게 조정.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.

### 후속 조정 2

- 사용자 추가 요청: 시간 숫자가 잘 보이지 않아 우선 크기를 키워달라고 요청.
- 적용한 변경 사항: `09:00 - 18:00` 시간 영역의 폰트 크기를 `0.92em`에서 `1.08em`으로 키우고, 숫자 칸 폭도 함께 늘려 잘림 없이 보이도록 조정.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.

## 2026-05-27 관리자 갤러리 업로드 안내 문구 정리

- 사용자 요청 요약: 갤러리 관리 업로드 안내의 `image/*`, `video/*` 같은 개발자 친화 문구를 없애고, 지원 파일 형식과 최대 용량만 알려주는 방식으로 변경 요청.
- 적용한 변경 사항: `components/admin/admin-gallery-manager.tsx`에서 안내 문구를 `지원 형식: PNG, JPG, JPEG, WebP, GIF, MP4, WebM, MOV. 최대 용량: 100MB`로 변경.
- 검증 결과: 서버 업로드 검증은 기존처럼 `image/*`, `video/*` 범주와 100MB 제한을 유지함을 확인. `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 다음 단계: 실제 허용 확장자를 더 엄격하게 제한해야 하면 서버 검증 로직도 확장자/MIME whitelist 방식으로 별도 조정 필요.

### 후속 조정

- 사용자 추가 요청: 갤러리 관리 업로드 버튼 옆 `업로드와 DB 저장까지 완료되면 목록에 즉시 반영됩니다.` 초록 안내 문구 제거 요청.
- 적용한 변경 사항: `components/admin/admin-gallery-manager.tsx`에서 해당 보조 안내 문구 삭제.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.

## 2026-05-27 운영관리 화면 보조 설명 정리 및 가독성 강화

- 사용자 요청 요약: 운영관리 화면 곳곳의 설명성 문구가 불필요하므로 제거하고, 갤러리 지원 형식 안내와 학생 관리 카드의 학생 정보 텍스트를 초록색이 아닌 검은색 계열로 선명하게 변경 요청.
- 적용한 변경 사항: 학생 관리, 장학금 지급관리, 운영 설정, 문자 템플릿 수동 발송, 현재 적용 중인 연락/문자 설정, 학생 추가 영역의 보조 설명 문구 삭제. 갤러리 업로드 지원 형식 문구를 진한 검은색 계열로 변경. 학생 관리 카드의 성별/학년, 실명, 소개 문구를 `subtle-text`에서 진한 검은색 계열 텍스트로 변경.
- 검증 결과: 삭제 요청 문구가 남아있지 않은 것을 `rg`로 확인. `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 다음 단계: 운영관리 전체 화면에서 여전히 흐린 보조 텍스트가 불편하면 남은 `subtle-text` 사용처를 화면별로 추가 정리.

## 2026-05-27 장학금 지급관리 필터 탭 및 정렬 변경

- 사용자 요청 요약: 장학금 지급관리 페이지 검색 아래에 책갈피 형태의 필터를 추가해 전체, 전액 장학금, 반액 장학금, 부분 장학금만 따로 볼 수 있게 하고, 전체 정렬은 부분장학금 → 반액장학금 → 전액장학금 순서 후 이름 가나다순으로 변경 요청.
- 적용한 변경 사항: `components/admin/admin-scholarships-manager.tsx`에 장학금 구분 필터 상태와 탭 UI 추가. 목록 정렬을 장학금 구분 우선순위(`부분장학금`, `반액장학금`, `전액장학금`) 후 학생 실명 기준 가나다순으로 변경.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.
- 다음 단계: 필터 탭 모양이 실제 화면에서 과하게 보이면 색상/높이를 더 낮춰 운영관리 화면 톤에 맞게 미세 조정.

### 후속 조정

- 사용자 추가 요청: 책갈피형 디자인보다 버튼형 필터가 운영관리 화면에 더 어울린다고 판단해 디자인 변경 요청.
- 적용한 변경 사항: 장학금 구분 필터의 책갈피형 `rounded-t`/하단선 제거. 일반 둥근 버튼 형태로 바꾸고 선택 상태만 초록 배경으로 강조.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과.

## 2026-05-27 관리자 공통 레이아웃 와이드 화면 정리

- 사용자 요청 요약: 운영관리 화면이 와이드 화면에서 왼쪽 공백이 비정상적으로 크고 본문/표가 오른쪽으로 밀려 보여, 현재 보이는 구조가 말이 안 된다고 지적. 나머지 관리자 페이지도 함께 수정 요청.
- 확인한 원인과 단서: 관리자 레이아웃이 공개 페이지용 `container-base(max-w-6xl)` 안에 들어가 있어 2560px 화면에서 전체 관리자 UI가 중앙으로 밀리고, 표는 좁은 본문 안에서 오른쪽으로 넘쳐 보였음.
- 적용한 변경 사항: `app/admin/(panel)/layout.tsx`에서 관리자 전용 풀폭 레이아웃으로 분리하고, 사이드바/본문 그리드를 `232~244px + minmax(0, 1fr)` 구조로 변경. 본문에 `min-w-0` 적용. `components/admin/admin-sidebar.tsx`, `components/admin/admin-topbar.tsx`의 카드 반경/간격/활성 상태를 운영툴에 맞게 낮춤. 상단바 제목은 현재 관리자 페이지 경로에 맞게 표시되도록 변경. `app/globals.css`에 `.admin-panel` 범위의 카드/표/입력 스타일을 추가해 관리자 전체 페이지에만 더 조밀한 스타일 적용.
- 검증 결과: `npm.cmd run -s lint` 통과. 첫 빌드는 라우트 출력까지 성공했지만 프로세스 종료 코드가 비정상으로 반환되어 재실행했고, 두 번째 `npm.cmd run -s build`는 종료 코드 0으로 통과. Chrome DevTools 방식으로 `/admin/sponsorships` 2560px 화면 확인 결과 사이드바 left 24px, 본문 left 284px, 표 right 2535px로 화면 폭 안에 정렬됨. `/admin/dashboard`, `/admin/students`, `/admin/scholarships`, `/admin/gallery`도 1440px 기준 `scrollWidth = innerWidth` 확인.
- 남은 위험 요소: 발송 관리처럼 내용이 긴 표는 셀 내부 텍스트 길이에 따라 여전히 가로 스크롤 영역이 생길 수 있으나, 공통 레이아웃 자체가 중앙으로 밀리는 문제는 해결됨.
- 다음 단계: 각 관리자 페이지별로 표 컬럼 밀도나 버튼 크기가 아직 답답하면 개별 표 단위로 컬럼 축약/줄바꿈 정책 추가 조정.

## 2026-05-27 운영 중심 관리자 대시보드 재구성

- 사용자 요청 요약: 관리자 대시보드가 개발자용 통계판처럼 보이고, 운영자가 오늘 해야 할 일보다 낮은 중요도의 숫자가 많아 실제 운영 중심 대시보드로 단순화 요청. DB 구조 변경 없이 기존 기능은 유지.
- 확인한 원인과 단서: 기존 대시보드는 전체 학생/문자/후원 방식/취소/갤러리 등 숫자 카드가 병렬로 나열되어 우선순위가 약했고, `오늘 연락 필요한 후원자`와 최근 신청 내역이 아래로 밀려 있었음.
- 적용한 변경 사항: `app/admin/(panel)/dashboard/page.tsx`를 운영 우선순위 구조로 재배치. 최근 30일 문자 발송/실패, 일시후원, 정기후원, 취소 신청 수는 대시보드 표시에서 제거. 핵심 지표는 전체 학생 수, 결연 완료 학생 수, 결연 대기 학생 수, 입금 대기 신청 수로 축소. 상단에 현재 결연률 progress와 오늘 연락 필요한 후원자 목록을 강조하고, 최근 신청 내역, 현재 운영 상태, 최근 갤러리 업로드를 이어 배치. 좌측은 결연률/최근 신청, 우측은 연락 필요/운영 상태/갤러리 구조로 조정.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. Chrome DevTools 방식으로 데스크톱 1440px 캡처 확인. 초기에는 오른쪽 연락 목록 높이 때문에 왼쪽 빈 공간이 생겨 2열 컬럼 구조로 재배치했고, 최근 신청 표의 가로 스크롤바를 줄이기 위해 `min-w-full`로 조정. 모바일에서는 표 내용이 컬럼 최소 폭을 밀어 전체 화면이 가로로 넘치는 문제가 확인되어 좌우 컬럼에 `min-w-0` 적용. 최종 모바일 기준 `scrollWidth = innerWidth = 390`, 첫 카드 폭 358px로 화면 안에 맞는 것 확인.
- 남은 위험 요소: 모바일의 최근 신청 표는 컬럼 특성상 카드 내부 가로 스크롤이 유지됨. 전체 페이지 넘침은 해결됐지만, 더 모바일 친화적으로 만들려면 표를 카드형 리스트로 별도 전환 가능.
- 다음 단계: 운영자가 최근 신청 내역도 모바일에서 표보다 카드형으로 보길 원하면 모바일 전용 리스트 레이아웃 추가.

## 2026-05-27 관리자 페이지 모바일 최적화

- 사용자 요청 요약: 지금까지 수정한 관리자 페이지를 모바일에서 보이는 기준으로 다시 최적화하고, 버튼 배치 등은 필요하면 변경해도 된다고 요청.
- 확인한 원인과 단서: Chrome DevTools 모바일 390px 기준으로 대시보드, 결연 신청 관리, 발송 관리, 학생 관리, 장학금 지급관리, 갤러리, 설정을 확인. 전체 페이지 가로 밀림은 대부분 없었지만, 결연 신청 관리는 모바일에서 넓은 표를 가로 스크롤해야 해 실제 조작성이 떨어졌고, 상단바 버튼 배치가 좁은 화면에서 어색하게 감겼음. 대시보드 최근 신청 내역도 모바일에서 표 형태가 답답했음.
- 적용한 변경 사항: `components/admin/admin-topbar.tsx`에서 모바일 상단 액션을 2열 버튼 배치로 정리하고 로그아웃 버튼은 한 줄 전체 폭으로 배치. `app/globals.css`에 관리자 모바일 전용 카드 반경, 버튼 높이/패딩, 표 셀 밀도 조정 추가. `components/admin/admin-sponsorships-manager.tsx`에 모바일 전용 결연 신청 카드 리스트 추가, 데스크톱에서는 기존 표 유지. 카드 안에서 후원자/학생/전화/이메일/학생 상태/메시지/상태 변경을 바로 확인하고 전화 걸기, 복사, 적용을 할 수 있도록 구성. `app/admin/(panel)/dashboard/page.tsx`의 최근 신청 내역도 모바일에서는 카드형 리스트로 표시하고 데스크톱에서는 표 유지.
- 검증 결과: `npm.cmd run -s lint` 통과, `npm.cmd run -s build` 통과. Chrome DevTools 모바일 390px에서 `/admin/dashboard`, `/admin/sponsorships`, `/admin/messages`, `/admin/students`, `/admin/scholarships`, `/admin/gallery`, `/admin/settings` 확인. 주요 페이지 최종 `scrollWidth = innerWidth = 390`, 외부로 튀는 요소 없음. 결연 신청 관리는 모바일에서 표 대신 카드 리스트로 표시되는 것 확인.
- 남은 위험 요소: 발송 관리의 문자 발송 이력 표는 화면 아래쪽에서 여전히 카드 내부 가로 스크롤을 사용함. 필요하면 별도 모바일 카드 리스트로 추가 전환 가능.
- 다음 단계: 실제 휴대폰에서 스크롤 길이가 부담되는 페이지가 있으면 학생/장학금 목록에 접기/펼치기 또는 페이지네이션을 추가 검토.

## 2026-05-27 관리자 대시보드 한 화면 압축

- 사용자 요청 요약: 대시보드 화면만큼은 한 스크린 안에 모든 정보가 들어오도록 블록을 더 촘촘히 배치해달라고 요청.
- 확인한 원인과 단서: 운영 중심 구조로 바꾼 뒤에도 1920x1080 기준에서는 오른쪽의 연락/운영 상태/갤러리 영역이 세로로 길어져 하단 일부가 접힐 수 있었음.
- 적용한 변경 사항: `app/admin/(panel)/dashboard/page.tsx`에서 대시보드 표시 개수를 최근 신청 3건, 오늘 연락 필요 2건, 최근 갤러리 1건으로 축소. 전체 데이터와 관리 기능은 유지하고 각 섹션의 전체 보기/관리 링크로 이동해 확인하도록 구성. `xl:` 기준 패딩, 간격, 글자 크기를 줄여 결연률, 최근 신청, 오늘 연락, 현재 운영 상태, 최근 갤러리 업로드가 한 화면에 들어오도록 압축.
- 검증 결과: `npm.cmd run -s lint`, `npm.cmd run -s build` 통과. Chrome 1920x1080 기준 `dashboardBottom=939`, `innerHeight=1080`, `fitsInViewport=true`, `scrollHeight=1080`으로 한 화면 안에 들어오는 것 확인. 2560x1268에서도 전체 블록 표시 확인.
- 남은 위험 요소: 화면 높이가 900px 이하인 데스크톱이나 브라우저 확대율이 높은 환경에서는 스크롤이 생길 수 있음.
- 다음 단계: 더 낮은 해상도까지 한 컷을 강제해야 하면 1366x768 전용 초압축 모드나 일부 섹션 접기 UI 추가 검토.

## 2026-05-27 디자인 스킬 기준 관리자 화면 재정리

- 사용자 요청 요약: 설치해둔 디자인 스킬 기준으로 모바일과 컴퓨터 관리자 페이지가 보이는 방식을 전반적으로 다시 다듬어달라고 요청.
- 확인한 원인과 단서: 관리자 화면이 공개 페이지의 부드러운 카드 스타일을 많이 공유해 운영툴치고 둥근 카드와 여백이 많았고, 모바일 메뉴는 현재 위치 표시가 약했음. 학생/장학금/설정 페이지에는 상단바 제목과 중복되는 별도 제목 카드가 남아 화면을 잡아먹었음.
- 적용한 변경 사항: `design-taste-frontend`와 `web-design-guidelines` 기준으로 관리자 전용 배경, 카드 반경, 표 밀도, 입력/버튼 터치 크기, 모바일 탭 메뉴를 재정리. `components/admin/admin-mobile-nav.tsx`를 추가해 모바일에서 현재 메뉴를 초록 버튼으로 표시. 상단바의 모바일 버튼은 2열로 줄이고, 데스크톱에서는 모바일 탭이 보이지 않도록 래퍼를 분리. 학생/장학금/설정 페이지의 중복 제목 카드를 제거. 문자 발송 이력은 모바일에서 표 대신 카드 리스트로 보이도록 추가. 주요 placeholder와 진행 문구도 웹 인터페이스 가이드에 맞춰 말줄임표를 정리.
- 검증 결과: `npm.cmd run -s lint`, `npm.cmd run -s build` 통과. Chrome CDP로 데스크톱 1440x1000 대시보드/결연 신청 관리 캡처 확인. 모바일 390x844 기준 `/admin/dashboard`, `/admin/sponsorships`, `/admin/messages`, `/admin/students`, `/admin/scholarships`, `/admin/gallery`, `/admin/settings` 모두 `scrollWidth = innerWidth = 390`으로 가로 넘침 없음 확인.
- 남은 위험 요소: 학생 관리와 장학금 지급관리는 61명 전체 카드/폼을 한 번에 렌더링하므로 모바일 세로 길이가 매우 김. 가로 깨짐은 없지만 실제 휴대폰 운영 편의성을 더 올리려면 페이지네이션, 접기/펼치기, 모바일 전용 요약 리스트가 필요할 수 있음.
- 다음 단계: 사용자가 학생/장학금 페이지의 긴 스크롤을 부담스러워하면 카드 접힘 상태를 기본값으로 두고, 수정/파일 관리는 펼쳤을 때만 보이게 추가 개선.

## 2026-05-27 결연 신청 관리 학생 실명 표시

- 사용자 요청 요약: 관리자 페이지의 결연 신청 관리 목록에서 학생 닉네임 옆에 본명을 괄호로 함께 표시 요청.
- 적용한 변경 사항: `components/admin/admin-sponsorships-manager.tsx`에 학생 표시명 helper를 추가해 닉네임과 실명을 `닉네임 (실명)` 형식으로 표시. 모바일 카드, 데스크톱 표, 응원 메시지 상세 모달에 동일 적용. 검색 대상에도 학생 실명을 추가해 실명으로도 결연 신청을 찾을 수 있게 함.
- 검증 결과: `npm.cmd run -s lint`, `npm.cmd run -s build` 통과.
- 남은 위험 요소: 실명이 미등록된 학생은 기존처럼 닉네임만 표시됨.
